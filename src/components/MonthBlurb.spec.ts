import { expect } from 'chai';

describe('Given GIVEN', () => {
    describe('When WHEN', () => {
        it('Then THEN', () => {
            // Given

            // When

            // Then
            expect(true).to.be.true;
        })
    })
})
